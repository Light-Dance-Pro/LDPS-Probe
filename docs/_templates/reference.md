# <Thing> Reference

<One-line scope: what this documents. If it summarises an authoritative source (e.g. a
firmware-side spec), link that source and note it wins on any discrepancy.>

> Reference = lookup material (lives in `reference/`). Keep it factual and scannable — tables,
> field lists, byte layouts, API rows — not narrative. One fact lives in one place; link, don't
> duplicate.

## <Section>
<Lookup content. Use tables for fields/parameters; keep prose minimal.>
